<% if @task.saved_change_to_status? %>
  <%= turbo_stream.remove dom_id(@task) %>
  <%= turbo_stream.append "tasks-#{@task.status}" do %>
    <%= render "task", task: @task %>
  <% end %>
<% else %>
  <%= turbo_stream.update dom_id(@task) do %>
    <%= render @task %>
  <% end %>
<% end %>
